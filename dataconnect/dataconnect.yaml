specVersion: "v1beta"
serviceId: "my-web-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "my-web-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
